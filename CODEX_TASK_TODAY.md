# CODEX_TASK_TODAY.md —— 2026-09-15 任务派发单（Codex 专属）

开工前请先读：PROJECT_STATE.md（必读）、协作约定.md。
你的产物一律写入 **results_codex/ 或 src_codex/**，不得修改 DSH 的文件和 data/ 目录。

## 任务 1（优先级最高）：独立复现 SKAB 基线
**不要阅读也不要复用 DSH 的脚本**（demo_pdm_v5.py 等），请自己从零实现，然后和下面的数字对比：

需要复现的两行（协议见 PROJECT_STATE.md 第 6 节）：
| 方法 | 事件检出率 | 误报分钟比例 | 延迟中位 |
|---|---|---|---|
| 固定报警值：逐信号稳健 z 分数，取最大，阈值按 0.5% 误报标定 | 82.4% | 19.7% | 60 s |
| PCA 重构误差：自适应标准化后的滑窗特征上做 PCA(90%方差)，重构误差为分数 | 88.2% | 15.9% | 0 s |

验收标准：
- 产出 results_codex/skab_repro.csv，逐文件列出 F1/检出/误报
- 产出 results_codex/skab_repro.md，包含对比表 + 偏差分析
- 偏差 ≤10%：写"复现成功"；10%~30%：列出你怀疑的协议差异并给实验证据；>30%：直接指出你认为 DSH 哪一步错了
- **只报你真实跑出来的数字**，跑不出就写跑不出

## 任务 2：MetroPT-3 数据集获取与体检（不要急着建模）
- 目标数据：MetroPT-3（UCI 数据集 791，巴西地铁空压机真实运行数据，含真实故障事件）
- 产物：results_codex/metropt3_audit.md，必须包含：
  1. 来源 URL、下载体积、采样率、时间跨度、字段清单与含义
  2. 真实故障事件的时间点（原始论文/数据集说明里的标注）
  3. 数据质量：缺测比例、异常值、是否需要重采样
  4. 可用性判断：能不能套用我们的统一协议？如果不能，说明需要改哪一步
  5. 前 200 行样例（脱敏即可，本来就是公开数据）
- 若 UCI 下载不通，尝试 Kaggle 镜像，并在报告里写清你最终用的是哪个源

## 任务 3：审查 DSH 的技术结论（挑刺）
只读 results/ 下的结果表和 实验日志.md，回答：
1. "误报分钟比例"这个指标会不会被故障基数（61%）误导？你觉得该用什么指标替代或补充？
2. 统一协议里"取文件前 40% 作为标定期"这个假设，在真实污水厂场景下成立吗？给出你会怎么改。
3. 如果让你从零设计这套预测性维护系统，你会先做什么、不做今天这些实验里的哪一件？
写进 results_codex/review_dsh.md，**允许尖锐**。

## 输出要求
1. 三个产物文件（skab_repro.md / metropt3_audit.md / review_dsh.md）
2. 在 实验日志.md 追加一条（用文件里给的模板，别改别人的内容）
3. 对人汇报用五段式：做了什么 / 结果 / 结论 / 失败与存疑 / 下一步
