

### 2026-09-22 DSH：实现方案 B —— 企业版离线报告（lanmai report）
- **做了什么**：新增 src/lanmai/report.py（225 行，只调用 core/pipeline 的同一套函数）+ cli.py 的 report 子命令：把「一份现场数据 + 一份已标定基线」渲染成**自包含单文件 HTML**（体检摘要 / 标定摘要 / P1·P2·P3 分级告警明细与分级说明 / 四张图 / 口径与局限 / 复现命令），图以 base64 内嵌、离线双击即开、数据不出本机；没装 matplotlib 时自动降级为无图报告。生成两份真实样例：demo/report_bsm1.html、demo/report_metropt3.html。
- **结果**：样例数字与手册第三节实测**完全一致** —— MetroPT-3（306,960 行 / 8 通道含 state 列）**105 个告警（P1 56 / P2 30 / P3 19）**；BSM1（11,520 行 / 9 通道）**6 个告警（P1 3 / P2 0 / P3 3）**、冻结通道首报 1970-01-27 19:15。run_all 新增 lanmai_report 阶段（快速链路 8 个阶段全通过，该阶段 9.3 秒）。
- **顺手修掉一个真实产品 bug**：inspect 遇到分类列（MetroPT-3 的 state）会因对字符串求均值而崩 —— 由报告生成器首次触发；已在 pipeline.inspect 把非数值列单独列出（报缺失率与取值数，不参与均值/零方差判定）。
- **判断**：这是「企业怎么用」的最短可演示闭环（数据进 → 报告出），且不动任何算法、不改任何已验证数字；对外口径仍是「本工具只做检测与标定，不含诊断与 RUL；阈值须现场标定」。
- **下一步**：可选做方案 A（本地 Web：上传 CSV 出同样的报告，约多半天，用于录视频）；否则进入材料轮（demo 页挂入口已做）。

### 2026-09-22 DSH：报告生成器 v1.1 —— 相对时间（1970 占位）口径修正
- **背景**：项目负责人打开 BSM1 样例报告发现「怎么都是 1970 年」。原因是仿真数据的时间列是**相对时间**（数值、起点 0），加载器按 epoch 秒处理 → 显示成 1970-01-01 起。真实数据（MetroPT-3）显示的是 2020 年真日期，不受影响。
- **做了什么**：report.py 升级 v1.1 —— 自动判定相对时间（首行年份 ≤ 1970），报告顶部加显著口径说明；KPI（时间跨度/首报）、体检段的时间范围、标定摘要的参考窗、告警明细的起止时间、以及四张图的横轴（含参考窗绿区与告警底色）**全部改用「第 X 天」**；真实日期数据完全不受影响。
- **结果**：BSM1 样例里日期型 1970-xx-xx 残留 0 处、「第 X 天」出现 20 处、时间跨度显示 119.99 天、首报显示「第 26.80 天」；MetroPT-3 样例保持真实日期（0 处相对时间提示）。
- **判断**：这是「仿真标仿真、现场标现场」的展示层纪律 —— 报告不能让评委/客户把仿真轨迹当成 1970 年的现场数据。
- **下一步**：等负责人决定是否做方案 A（本地 Web 上传 CSV → 同一份报告）。

### 2026-09-22 DSH：实现方案 A —— 本地工作台（lanmai serve）
- **做了什么**：新增 src/lanmai/server.py（纯 Python 标准库 http.server，页面用纯表单、无 JS）+ cli.py 的 serve 子命令：① 上传健康期数据标定基线（可用多段/工况分层）② 上传待监测数据 + baseline 出企业版报告 ③ 样例一键试跑 ④ 本机产物列表。另写可复现冒烟脚本 src/dsh/2026-09-22_11_lanmai_serve_smoketest.py（启服务→首页→样例出报告→上传出报告→上传标定→校验产物→关服务）。
- **结果**：冒烟 6/6 通过；run_all 新增 lanmai_serve_smoke 阶段（快速链路 9 个阶段）；安全默认 = 仅 127.0.0.1 / 上传上限 300MB / 只收 csv,csv.gz,txt,json / 文件名只取 basename；上传的原始数据单独放 upload/ 子目录、不再混进产物列表（冒烟时发现并修掉）。
- **判断**：方案 A 与 CLI 完全同源（同一套 core/pipeline/report 函数），算法与阈值口径不因界面变化；定位是「内部工具」，无账号/权限/多租户，数据刻意不出本机（符合工控网习惯），对外表述为「本地部署、可选局域网访问」。
- **下一步**：用它录 3/8 分钟路演视频的「上传数据 → 出告警 → 出报告」段落；随后进入材料轮（说明书/BP）。

### 2026-09-22 DSH：Windows 上手体验修复（不需要 PYTHONPATH）
- **背景**：项目负责人在 PowerShell 里按文档粘 `export PYTHONPATH=src` 报错（PowerShell 没有 export），且当时不在仓库目录，导致 No module named lanmai。
- **做了什么**：① 新增 run_lanmai.py（一键入口：自动把 src/ 加进 sys.path 并切到仓库根，无需任何环境变量）；② 新增 start_lanmai.cmd（双击即起本地工作台并开浏览器）；③ 新增 pyproject.toml（可选的可编辑安装：pip install -e . 之后任何目录可用 python -m lanmai / lanmai 命令）；④ docs/10 新增第九章「Windows 快速上手」，给出四种方式并明确 PowerShell 的正确写法（$env:PYTHONPATH）与中文名启动器改 ASCII（start_lanmai.cmd）的原因。
- **结果**：四种方式全部实测通过 —— ① python run_lanmai.py --help（列出 inspect/calibrate/watch/report/serve/selftest）；② 从 C:\Users\boyi 运行 run_lanmai.py selftest 通过；③ 双击 start_lanmai.cmd 后首页 HTTP 200；④ pip install -e . 后从任意目录 python -m lanmai --help 与 lanmai --help 均可用。
- **判断**：这是「交付给人用」这一层的必要修复 —— 文档里所有命令都要能在 Windows/PowerShell 直接粘贴执行。
- **下一步**：材料轮（说明书/BP/视频脚本）。
