# Q10：端到端管线 P1–P5 修正落地审计（Codex，2026-09-20）

审计对象：

- `src/dsh/2026-09-18_14_pipeline.py`
- `results/2026-09-18/dsh/pipeline_summary.json`
- `results/2026-09-18/dsh/pipeline_run.md`
- 对照：`results/2026-09-18/dsh/metropt3_metrics_frozen_dsh.json`、`PROJECT_STATE.md` §16/§19

未执行会覆盖 DSH 产物的管线命令；对检测事件机另做了**只读内存复算**，不写任何 DSH 路径。

## 总表

| 项 | 判定 | 核心证据 |
|---|---|---|
| P1 阈值来源文案 | **已生效** | 源码 21–24、55、98、110–111 行；`pipeline_run.md` 13–15、48 行 |
| P2 独立前瞻验证声明 | **已生效** | 源码 99 行；`pipeline_run.md` 49 行明确写“否” |
| P3 RUL 门禁事后性与在线禁用声明 | **部分生效** | 源码 70 行同时具备两项；持久化报告 55 行只保留“事后(retrospective)”，没有“不可在线” |
| P4 诊断改标数据集级占位 | **已生效** | 源码 57–61、112 行；summary 的 `诊断` 字段及告警表均为占位文案 |
| P5 冻结误报口径、201 与机器可读元数据 | **已生效** | 源码 52、109–115 行；只读复算为 201；summary 三类字段齐全 |

## P1：阈值来源文案

**判定：已生效。**

- `src/dsh/2026-09-18_14_pipeline.py:21-24` 明确区分：`THR=2.395` 是官方 4 故障窗标签辅助选出的 DET 工作点；无标签 0.995 标定分位为 `13.139`。
- `src/dsh/2026-09-18_14_pipeline.py:55` 的运行期输出再次声明标签辅助、同集表现及无标签参照。
- `src/dsh/2026-09-18_14_pipeline.py:98` 的自动审计项保存同一结论。
- `results/2026-09-18/dsh/pipeline_run.md:13-15,48` 已持久化：阈值 `2.395`、性质为“标签辅助 DET 工作点（同集表现）”、无标签工作点 `13.139`。

精度备注：冻结 JSON 保存的是 `2.3954` 与 `13.1393`，管线展示为三位小数。只读复算显示 `2.395` 和 `2.3954` 在当前分数流上均产生 205 个事件、201 个误报，因此当前舍入没有改变整数结果。

## P2：自动审计新增“是否独立前瞻验证 = 否”

**判定：已生效。**

- `src/dsh/2026-09-18_14_pipeline.py:99` 新增检查项 `是否独立前瞻验证`，结论以“否”开头，说明 4 个故障窗同时用于选点与汇报。
- `results/2026-09-18/dsh/pipeline_run.md:49` 已把该行写入最终自审计表，`是否通过=False`。
- `results/2026-09-18/dsh/pipeline_summary.json` 的 `备注` 也保存“非独立前瞻验证”。

这不是只改注释：最终 Markdown 与 JSON 都能看到声明。

## P3：RUL 门禁标注 retrospective 且不得用于在线决策

**判定：部分生效。**

- **源码运行输出完整：** `src/dsh/2026-09-18_14_pipeline.py:70` 明确写“本条为事后门禁 retrospective，使用了告警时点之后的数据，不可用作在线判据”。
- **自动审计仅完成一半：** `src/dsh/2026-09-18_14_pipeline.py:105` 和 `results/2026-09-18/dsh/pipeline_run.md:55` 只写“该门禁为事后(retrospective)”，没有把“使用未来数据 / 不可用于在线决策”持久化到报告。
- `pipeline_summary.json` 的 `RUL` 对象也没有门禁性质或在线适用性字段。

因此代码的标准输出层面已修正，但交付报告/机器可读摘要没有完整承载第二半约束。建议把审计行改为“该门禁使用告警后数据，为事后(retrospective)，不可用于在线决策”，并在 summary 增加例如 `门禁可在线使用: false`。

## P4：诊断输出改为数据集级故障类型占位

**判定：已生效。**

- `src/dsh/2026-09-18_14_pipeline.py:57-60` 对 MetroPT-3 返回：`空气泄漏（数据集级故障类型占位：官方四事件均为该模式，且不区分误报，非训练所得诊断器）`。
- `src/dsh/2026-09-18_14_pipeline.py:61` 将该文本写入每条告警；`:112` 写入摘要。
- `results/2026-09-18/dsh/pipeline_run.md:18` 及 63–72 行可见相同占位声明。

输出已明确不是模型推断结果；不是单纯改注释。

## P5：误报冻结口径与摘要元数据

**判定：已生效。**

### 误报定义和数字

- `src/dsh/2026-09-18_14_pipeline.py:52` 直接按“告警起点不在任一 `[g0-60min, g1]` 命中窗”计误报。
- 对当前分数流独立只读复算：

```text
threshold=2.395  -> alarm_events=205, fp_events=201
threshold=2.3954 -> alarm_events=205, fp_events=201
```

- `pipeline_summary.json` 为 201；冻结 `metropt3_metrics_frozen_dsh.json` 的 `det_best.fp_events` 也是 201，二者一致。

### 机器可读元数据

`src/dsh/2026-09-18_14_pipeline.py:109-115` 计算/写入；`pipeline_summary.json` 实际包含：

- `cost_parameters_are_placeholders: true`
- `口径版本: "PROTOCOL_v1.5 + MetroPT-3 冻结口径 2026-09-18"`
- `输入哈希_分数流: "ade8c0e00922"`

独立重算输入 gzip 文件 MD5 前 12 位同为 `ade8c0e00922`。

## 最终判断

P1、P2、P4、P5 均已在源代码和交付产物中真正落地；P3 在源码打印语句中完整落地，但生成的 Markdown 自审计与 JSON 摘要只保留了“事后”标签，未保留“使用告警后数据、不可在线”的关键限制，故判为**部分生效**。除 P3 的持久化缺口外，没有发现“只改文案而数字/口径未变”的情况；P5 的 201 已由独立事件机只读复算验证。
