@echo off
chcp 65001 >nul
cd /d "%~dp0"
set PYTHONPATH=%CD%\src
echo ============================================================
echo 澜脉 · 本地工作台（浏览器上传数据 -^> 标定基线 / 出报告）
echo 数据不出本机；关闭这个窗口即停止服务。
echo 若浏览器没自动打开，请手动访问 http://127.0.0.1:8765/
echo ============================================================
python -m lanmai serve --open
pause
