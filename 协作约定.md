# 协作约定.md —— 多个 AI 助手如何并行工作

## 1. 目录与文件所有权（最重要）
    tasks/YYYY-MM-DD_codex.md     当天的任务单（DSH 起草，人确认）
    src/dsh/YYYY-MM-DD_*.py       DSH 的脚本
    src/codex/YYYY-MM-DD_*.py     Codex 的脚本
    results/YYYY-MM-DD/dsh/       DSH 的产物（表 / 图 / 报告）
    results/YYYY-MM-DD/codex/     Codex 的产物
    logs/实验日志.md              两个助手都往里追加（只增不改）
任何人不得修改别人目录下的文件。需要改，就写在自己目录里。

|  | DSH（DeepSeek Harness） | Codex（GPT） |
|---|---|---|
| 定位 | 主实现：建模、实验、BP 框架 | 独立验证 + 数据获取 + 补位实现 |
| 强项用法 | 连续迭代、多方案对比、写作 | 换一套脑子重跑、审查假设、找漏洞 |

核心机制：同一个关键实验，两边各写一遍代码。数字对得上 → 结论可信；对不上 → 写差异报告，这本身就是最有价值的产出。

## 2. 硬性规则
1. 只读对方的文件，不改。
2. 不动 data/ 目录。
3. 所有结论必须附"可复现命令 + 输出路径"。
4. 禁止编造数据、禁止使用"龙渊/DeepLong"的任何素材。
5. 每天收工前在 logs/实验日志.md 追加一条（模板见该文件）。
6. "我没跑出来 / 结果不如预期"必须写进日志，不许只报喜。

## 3. 每日循环（2026-09-16 起固定）
1. 中午：DSH 生成 tasks/当天日期_codex.md（谁都可以提任务，DSH 落地成单）。两个助手开工前先读 PROJECT_STATE.md。
2. 下午：各自跑，各自写进 results/当天日期/dsh（或 codex）/ 与 logs/实验日志.md。
3. 晚上：人把 Codex 的日志贴回 DSH；DSH 合并结论、更新 PROJECT_STATE.md 第 8 节任务表与第 12 节已冻结结论。
4. 任何数字要进 BP，必须先过「协议冻结 + 双方独立实现比对」这一关。

## 4. 交叉验证判定标准
- 偏差 ≤10% → 复现成功，结论可用。
- 偏差 10%~30% → 必须定位到具体差异（特征、标定期、判决粒度、随机种子），写进日志。
- 偏差 >30% → 以 PROJECT_STATE.md 第 5 节的统一协议为准，逐条对齐后重跑。

## 5. 版本管理（已由 DSH 初始化，人不需要懂 git）
- 提交命令：
      cd C:\Users\boyi\Desktop\BEWG_PdM
      git add -A
      git commit -m "dayN: 一句话说明今天做了什么"
- 远端：比赛评审期间保持私有仓库（公开会影响专利新颖性，也等于把进度给对手看）；赛事结束后再决定是否转公开。
- data/ 与 _readout/ 已被 .gitignore 排除。
- 靠"目录隔离"避免冲突，不使用分支，降低出错概率。


5. 推送权限（2026-09-17 项目负责人确认）：DSH 每完成一个工作块即 commit；当天收工前**直接 git push，无需逐次询问**。
   若推送失败（网络中断、凭据失效、远端冲突），必须在回话里明确说明失败原因与当前差异，不得静默。
   Codex 若恢复，同规则适用；任何一方不得 force push、不得重写历史。


6. 推送失败的处置（2026-09-18 实战确认）：本项目 git 走本机代理 127.0.0.1:7897，代理掉线时 push 会报 Failed to connect ... via 127.0.0.1。此时先绕过代理重试：
       git -c http.proxy= -c https.proxy= push origin main
   （GitHub 直连通常可达）。仍失败才向项目负责人报告，并说明本地领先远端几个提交。

## 6. 沟通格式（每个助手对人汇报时必须包含）
1. 我做了什么（脚本 + 命令）
2. 结果（表格/数字）
3. 我的结论（不超过 3 条）
4. 我不确定 / 失败的地方
5. 建议下一步

## 7. 交接规则（2026-09-18 起，项目负责人要求，替代原转达规则）
1. **所有交接走 handoff/ 文件**，命名 YYYY-MM-DD_<发件人>_to_<收件人>[_rN].md；只增不改。
2. DSH 在回话里**只给一行指针**（文件名 + 一句话要点），**不再输出大段可复制文本**——避免占用 Codex 侧 token。
3. 任务派出走 tasks/YYYY-MM-DD_codex.md；对方只需读这两个目录，不需要聊天转述全文。
4. 每条消息结尾必须有「需要对方做什么」，以及产物落盘路径；没有明确请求的消息视为信息同步，对方不必回应。
5. Codex 回话同理：写 handoff/YYYY-MM-DD_codex_to_dsh[_rN].md，只把文件名告诉项目负责人即可。

## 8. Codex 使用原则（2026-09-18 起，成本约束）
1. 攒批：Codex 一次会话只做一到两大块任务，清单写进 tasks/<日期>_codex.md，避免一轮一轮对话。
2. 只派必须由第二套独立实现完成的活：独立实现/复核、交叉验证、口径审计。实现、迭代、调试、写作、图表由 DSH 承担。
3. 任务自包含：数据路径、口径、产物路径、验收标准、禁止事项全部写在文件里。
4. 人的转达只发文件名（handoff/ 与 tasks/ 下的），不发正文。

## 9. 文件写入纪律（2026-09-21 事故后新增，所有助手必须遵守）
1. **绝不在写入表达式里做可能失败的计算**。反例（已造成事故）：io.open(p,'w').write(可能抛异常的表达式) —— open(...,'w') 会**先把文件清空**，表达式再抛异常，文件内容即永久丢失。
2. **批量改文档一律用「先算好内容 → 写临时文件 → os.replace 原子替换」**；改完立即核对行数与章节标题。
3. 改完 PROJECT_STATE.md 后必须跑一次一致性自检（python src/dsh/2026-09-18_18_consistency_check_v2.py），它会核对文档引用路径、日志引用脚本与冻结数字。
4. 提交前用 git show HEAD:<文件> | wc -l 与工作区对比一次，确认没有文件被意外截断。
