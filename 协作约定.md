# 协作约定.md —— 多个 AI 助手如何并行工作

## 1. 目录与文件所有权（最重要）
    tasks/YYYY-MM-DD_codex.md     当天的任务单（DSH 起草，人确认）
    src/dsh/YYYY-MM-DD_*.py       DSH 的脚本
    src/codex/YYYY-MM-DD_*.py     Codex 的脚本
    results/YYYY-MM-DD/dsh/       DSH 的产物（表 / 图 / 报告）
    results/YYYY-MM-DD/codex/     Codex 的产物
    logs/实验日志.md              两个助手都往里追加（只增不改）
任何人不得修改别人目录下的文件。需要改，就写在自己目录里。

|  | DSH（DeepSeek Harness） | Codex（GPT） |
|---|---|---|
| 定位 | 主实现：建模、实验、BP 框架 | 独立验证 + 数据获取 + 补位实现 |
| 强项用法 | 连续迭代、多方案对比、写作 | 换一套脑子重跑、审查假设、找漏洞 |

核心机制：同一个关键实验，两边各写一遍代码。数字对得上 → 结论可信；对不上 → 写差异报告，这本身就是最有价值的产出。

## 2. 硬性规则
1. 只读对方的文件，不改。
2. 不动 data/ 目录。
3. 所有结论必须附"可复现命令 + 输出路径"。
4. 禁止编造数据、禁止使用"龙渊/DeepLong"的任何素材。
5. 每天收工前在 logs/实验日志.md 追加一条（模板见该文件）。
6. "我没跑出来 / 结果不如预期"必须写进日志，不许只报喜。

## 3. 每日循环（2026-09-16 起固定）
1. 中午：DSH 生成 tasks/当天日期_codex.md（谁都可以提任务，DSH 落地成单）。两个助手开工前先读 PROJECT_STATE.md。
2. 下午：各自跑，各自写进 results/当天日期/dsh（或 codex）/ 与 logs/实验日志.md。
3. 晚上：人把 Codex 的日志贴回 DSH；DSH 合并结论、更新 PROJECT_STATE.md 第 8 节任务表与第 12 节已冻结结论。
4. 任何数字要进 BP，必须先过「协议冻结 + 双方独立实现比对」这一关。

## 4. 交叉验证判定标准
- 偏差 ≤10% → 复现成功，结论可用。
- 偏差 10%~30% → 必须定位到具体差异（特征、标定期、判决粒度、随机种子），写进日志。
- 偏差 >30% → 以 PROJECT_STATE.md 第 5 节的统一协议为准，逐条对齐后重跑。

## 5. 版本管理（已由 DSH 初始化，人不需要懂 git）
- 提交命令：
      cd C:\Users\boyi\Desktop\BEWG_PdM
      git add -A
      git commit -m "dayN: 一句话说明今天做了什么"
- 远端：比赛评审期间保持私有仓库（公开会影响专利新颖性，也等于把进度给对手看）；赛事结束后再决定是否转公开。
- data/ 与 _readout/ 已被 .gitignore 排除。
- 靠"目录隔离"避免冲突，不使用分支，降低出错概率。

## 6. 沟通格式（每个助手对人汇报时必须包含）
1. 我做了什么（脚本 + 命令）
2. 结果（表格/数字）
3. 我的结论（不超过 3 条）
4. 我不确定 / 失败的地方
5. 建议下一步

## 7. 转达规则（2026-09-17 起，王家兴要求）
1. 只要 DSH 需要 Codex 做任何事，必须在回话末尾给出一段**可直接复制**的文本，标题统一为《转给 Codex》。
2. 同一段内容同时落盘到 handoff/YYYY-MM-DD_dsh_to_codex_rN.md，保证文件与聊天内容一致。
3. 文本里必须写清：读哪个文件、做什么、产物放哪、什么时候要、以及不要做什么。
4. 反过来，Codex 需要 DSH 做事时，同样在它汇报末尾给一段《转给 DSH》，由人转达。
