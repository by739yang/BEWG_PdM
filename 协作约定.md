# 协作约定.md —— 两个 AI 助手（DSH 与 Codex）如何并行工作

## 1. 角色分工（避免重复劳动，也避免互相污染）
| | DSH（DeepSeek Harness） | Codex（GPT） |
|---|---|---|
| 定位 | 主实现：建模、实验、BP 框架 | 独立验证 + 数据获取 + 补位实现 |
| 产物目录 | results/ 、根目录 *.py | **results_codex/ 、src_codex/** |
| 强项用法 | 连续迭代、多方案对比、写作 | 换一套脑子重跑、审查假设、找漏洞 |

**核心机制：同一个关键实验，两边各写一遍代码。** 数字对得上 → 结论可信；对不上 → 写差异报告，这本身就是最有价值的产出（说明协议里有隐含假设）。

## 2. 硬性规则
1. 只读对方的文件，**不改**。要改就写在自己的目录里。
2. 不动 data/ 目录。
3. 所有结论必须附"可复现命令 + 输出路径"。
4. 禁止编造数据、禁止使用"龙渊/DeepLong"的任何素材。
5. 每天收工前在 实验日志.md **追加**一条（不改别人写的）。模板见该文件。
6. 任何"我没跑出来/结果不如预期"都要写进日志，不许只报喜。

## 3. 每日循环（人只需要做 3 步）
1. 早上：把 CODEX_TASK_TODAY.md 内容替换成当天任务（可由 DSH 起草），两个助手都先读 PROJECT_STATE.md。
2. 白天：两边各自跑，各自往 results*/ 和 实验日志.md 写。
3. 晚上：把 Codex 的日志贴回 DSH（或反过来），让 DSH 做合并、更新 PROJECT_STATE.md 的"任务表"和"结论区"。

## 4. 交叉验证的判定标准
- 同一指标两边偏差 **≤10%** → 视为复现成功，结论可用。
- 偏差 **10%~30%** → 必须定位到具体差异（特征、标定期、判决粒度、随机种子），写进日志。
- 偏差 **>30%** → 按协议冲突处理：以 PROJECT_STATE.md 第 6 节的统一协议为准，逐条对齐后重跑。

## 5. 文件版本管理建议
强推 git（本地即可）：
    cd C:\Users\boyi\Desktop\BEWG_PdM
    git init
    git add -A && git commit -m "day1: SKAB baselines"
两个助手各占一个分支（dsh / codex），人每天合并一次。这样能避免"谁覆盖了谁"，也留下完整的实验轨迹——答辩时这就是"研发过程证据"。

## 6. 沟通格式（每个助手对人汇报时必须包含）
1. 我做了什么（脚本 + 命令）
2. 结果（表格/数字）
3. 我的结论（不超过 3 条）
4. 我不确定/失败的地方
5. 建议下一步（可勾选）
